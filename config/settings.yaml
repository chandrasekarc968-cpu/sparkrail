kafka:
  bootstrap_servers: "localhost:9092"
  topic: "railway_events"

spark:
  app_name: "RailwayBlockPlanning"
  master: "local[*]"

database:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "password"
  dbname: "railway_db"

optimization:
  solver: "PULP_CBC_CMD"
  time_limit: 3600

ml:
  xgboost_model_path: "models/xgboost_criticality.json"

rl:
  gnn_model_path: "models/gnn_network.pt"
